{{ form_start(form) }}

        <div class="mb-3">
            {{ form_row(form.nom) }}
        </div>
        <div class="mb-3">
            {{ form_row(form.prenom) }}
        </div>
        <div class="mb-3">
            {{ form_row(form.mail) }}
        </div>
        <div class="mb-3">
            {{ form_row(form.password) }}
        </div>
        <div class="d-grid gap-2 d-sm-block">
            <button class="btn btn-danger">{{ button_label|default('Inscrption') }}</button>
            <a class="btn btn-secondary" href="{{ path('home') }}">Retour</a>
        </div>

{{ form_end(form) }}
