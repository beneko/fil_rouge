{% extends 'base.html.twig' %}

{% block title %}Categories{% endblock %}

{% block body %}
    <h1>Categories {{ category.nomCat }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Catégorie</th>
                <td>{{ category.nomCat }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ category.libelleCat }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('categories_index') }}">back to list</a>

    <a href="{{ path('categories_edit', {'id': category.id}) }}">edit</a>

    {{ include('categories/_delete_form.html.twig') }}

    <div id="carouselExemple" class="carourel slide" data-ride=""
{% endblock %}
