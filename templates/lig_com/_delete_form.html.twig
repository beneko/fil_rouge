<form method="post" action="{{ path('lig_com_delete', {'id': lig_com.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ lig_com.id) }}">
    <button class="btn">Delete</button>
</form>
